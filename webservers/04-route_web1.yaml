apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: web1
    app.kubernetes.io/component: web1
    app.kubernetes.io/instance: web1
  name: web1
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: web1
    weight: 100
  wildcardPolicy: None
